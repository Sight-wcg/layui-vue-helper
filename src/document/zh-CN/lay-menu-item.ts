import { LayDocument } from '../../shared/types';
import { DocumentSlot } from '../../shared/types';

export const slots: DocumentSlot[] = [{ name: 'title', description: '菜单标题' }];

export const document: LayDocument = { slots };

export default document;
